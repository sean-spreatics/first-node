<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Test page</title>

    <!-- bootstrap cdn -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" />

    <!-- css -->
    <link rel="stylesheet" href="/static/css/test.css" />
  </head>
  <body>
    <!-- <h1>여기는 test page! 안뇽</h1> -->
    <%- include('./header.ejs') %>

    <!-- # ejs 문법 -->
    <!-- 1) JS 코드 -->
    <% for (let i = 0; i < 3; i++) { %>
    <h1>여기는 test page! 안뇽</h1>
    <% } %>

    <!-- 2) 변수값 출력 -->
    <% let username = 'Sean';%>
    <p><%= username %></p>

    <% const users = ['userA', 'userB', 'userC']; for (let i = 0; i < users.length; i++) { %>
    <h2><%= users[i]; %></h2>
    <% } %>

    <!-- 3) include(view의 상대 주소)-->
    <%- include('./card.ejs') %>

    <!-- # express static 사용하기 -->
    <!-- static: image, css, javascript 파일 제공 역학 (정적 파일) -->
    <img src="/static/image/apple.jpg" alt="apple" width="300" />

    <!-- bootstrap cdn -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js
    "></script>

    <!-- js -->
    <script src="/static/js/test.js"></script>
  </body>
</html>
